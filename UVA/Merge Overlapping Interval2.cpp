#include<bits/stdc++.h>
using namespace std;



int main()
{
    vector<int> setF{1,2,3,4,5,6,15,2};
    vector<int> setL{10,9,8,7,6,6,20,3};

    int l = setF.size();
    if(l != setL.size())
        return 0;
    sort(setF.begin(), setF.end());
    sort(setL.begin(), setF.end());


    for(int i = 0 ; i< l ; i++)
    {
        j = i;
        while(setF[])
    }

}
